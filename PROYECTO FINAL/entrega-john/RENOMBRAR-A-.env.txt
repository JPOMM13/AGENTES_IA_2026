# ⚠️  ESTE ARCHIVO DEBE RENOMBRARSE A  .env  (con el punto delante y sin .txt)
#     macOS/Linux:  mv 'RENOMBRAR-A-.env.txt' .env
#     Windows:      ren "RENOMBRAR-A-.env.txt" .env

# Credenciales del proyecto — Agente de Eventos (Grupo 5)
# John Manchego
# Usuario PERSONAL: no lo compartas. Si se pierde, se revoca solo el tuyo.
# NUNCA subas este archivo ni la carpeta certs/ al repositorio.

DB_HOST=34.176.55.43
DB_PORT=5432
DB_NAME=eventos
DB_USER=john
DB_PASSWORD=Spim0o7fhNN5suzxe7on

DB_SSLMODE=verify-ca
DB_SSLROOTCERT=certs/server-ca.pem
DB_SSLCERT=certs/client-cert.pem
DB_SSLKEY=certs/client-key.pem

DATABASE_URL=postgresql://john:Spim0o7fhNN5suzxe7on@34.176.55.43:5432/eventos?sslmode=verify-ca&sslrootcert=certs/server-ca.pem&sslcert=certs/client-cert.pem&sslkey=certs/client-key.pem

# Modelo (Google AI Studio, no requiere GCP): https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
